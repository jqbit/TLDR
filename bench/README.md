# bench

One-off research and analysis scripts (token analysis, charts, DSPy
experiments, the historical `v0.14-bench.sh`). Nothing here runs in CI —
the CI gates formerly in this directory now live in [`tests/`](../tests/).

The canonical, reproducible measurement harness is [`evals/`](../evals/).
